{% extends 'plain.html.twig' %}

{% block body %}
			<div class="container vh-100 d-flex flex-column justify-content-center align-items-center">
    <h1>Acción no valida</h1>

    <p>
        Denegado el acceso a la acción que se solicita. Prueba a <a href="{{ path('inicio') }}">volver a la página de inicio</a> o a <a href="{{ path('user_logout') }}">cerrar sesión</a>.
    </p>
    {{exception.message}}
    </div>
{% endblock %}
